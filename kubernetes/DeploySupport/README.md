# How To Use KubePython Docker For Develope Work #

run below command to mount kube-python package into KubePython Docker Image

```
docker run -it -v ./kube-python:/kube-python kubepython:v1 xxx xxx xxx xxx xxx
```